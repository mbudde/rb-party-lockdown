To install the plugin run:
    ./waf configure
    ./waf install

This defaults to installing the plugin into the rhythmbox plugin directory in
'~/.gnome2/rhythmbox/plugins'. To install the plugin system wide use
    ./waf --destdir=/usr/lib/rhythmbox/plugins install
or
    DESTDIR=/usr/lib/rhythmbox/plugins ./waf install
